@model List<DAL.Cardapios>
@{
    ViewData["Title"] = "Index";
    List<DAL.Cardapios> Pratos = ViewBag.Cardapios;
}

<div class="row">
    @foreach (var prato in Model)
    {
        <table>
            <tr>
                <td>@prato.Id</td>
                <td>@prato.Nome</td>
                <td>@prato.Descricao</td>
                <td><img src="@prato.Imagem" /></td>
                
            </tr>
        </table>
        @*<div class="col-3 mb-3">
            <a class="card d-block" href="@Url.Action("Detail", "Product", new { Id = product.Id })">
                <span class="card-body d-block">
                    <img src="@prato.Image" />
                    @prato.Name (@prato.Category.Name)
                </span>
            </a>
        </div>*@
    }
</div>
